services:
  contabilizador:
    build: .
    env_file: .env
    environment:
      WEB_HOST: "0.0.0.0"
      WEB_PORT: "8000"
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
